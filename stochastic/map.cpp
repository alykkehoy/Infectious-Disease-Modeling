#include <iostream>
#include "person.h" //for storing person objects

class map(unsigned int width, unsigned int height) {

  Person map[width][height];
   

}
//the map object, we'll start out as a matrix for now
