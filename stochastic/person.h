#ifndef GUARD_PERSON_H
#define GUARD_PERSON_H

class Person
{
    enum State {S,I,R};
     

public:
    Person(enum s);

    enum getState();

    enum::setState(enum s);
    
};



#endif // GUARD_DISEASE_H
